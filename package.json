{
  "devDependencies": {
    "jest": "^23.0.0",
    "lerna": "^2.9.0"
  },
  "private": true,
  "scripts": {
    "build": "lerna run build",
    "validate": "lerna run validate"
  },
  "workspaces": {
    "packages": [
      "packages/*"
    ],
    "nohoist": [
      "**/*"
    ]
  }
}
